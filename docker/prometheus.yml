global:
  scrape_interval: 5s
  scrape_timeout: 4s
  evaluation_interval: 15s

scrape_configs:
  - job_name: devpush-deployments
    honor_labels: false
    static_configs:
      - targets:
          - metrics-exporter:9100
